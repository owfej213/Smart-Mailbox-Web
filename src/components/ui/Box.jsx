import styled from '@emotion/styled';
import { color, space, layout, position, typography, border, flexbox } from 'styled-system';

const Box = styled.div`
  ${color}
  ${space}
  ${layout}
  ${position}
  ${typography}
  ${border}
  ${flexbox}
  display: flex;
`

export default Box;